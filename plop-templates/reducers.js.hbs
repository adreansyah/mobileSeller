const initialState = {
    /*
        set initial state
    */
}
export const {{name}} = (state = initialState , action) => {
  switch (action.type) {
    case 'TYPE_NAME':
      return {
        /* set return */
        ...state,
      }
    default:
      return state;
  }
};